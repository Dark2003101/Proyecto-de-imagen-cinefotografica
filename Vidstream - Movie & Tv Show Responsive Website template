<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Vidstream - Movie & Tv Show Responsive Website template</title>

<style>
  :root {
    --img: url("c:\Users\Maycol\Downloads\WhatsApp Image 2026-02-16 at 3.57.15 PM.jpeg");
  }

  html, body {
    height: 100%;
    margin: 0;
    background: transparent;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  body::before{
    content: "";
    position: fixed;
    inset: 0;
    background: var(--img) center/cover no-repeat;
    filter: blur(30px) brightness(0.6);
    transform: scale(1.06);
    z-index: -2;
    will-change: transform, filter;
    pointer-events: none;
  }

  body::after{
    content: "";
    position: fixed;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    background:
      radial-gradient(1200px 600px at 50% 50%, rgba(0,0,0,0) 40%, rgba(0,0,0,0.35) 100%),
      linear-gradient(180deg, rgba(0,0,0,0.18) 0%, rgba(0,0,0,0) 20%, rgba(0,0,0,0) 80%, rgba(0,0,0,0.18) 100%);
    mix-blend-mode: multiply;
  }

  img{
    max-width: 100%;
    max-height: 100%;
    width: auto;
    height: auto;
    object-fit: contain;
    z-index: 1;
    display: block;
    user-select: none;
    -webkit-user-drag: none;
    pointer-events: none;
  }

  img{ opacity: 0; transform: translateY(6px); transition: opacity .45s ease, transform .45s ease; }
  img.loaded{ opacity: 1; transform: none; }
  
</style>
</head>
<body>
  <img id="mainImg" src="c:\Users\Maycol\Downloads\WhatsApp Image 2026-02-16 at 3.57.15 PM.jpeg" alt="Imagen cinematográfica">
<script>
  const img = document.getElementById('mainImg');
  if(img.complete) img.classList.add('loaded');
  else img.addEventListener('load', ()=> img.classList.add('loaded'));
</script>
</body>
</html>
