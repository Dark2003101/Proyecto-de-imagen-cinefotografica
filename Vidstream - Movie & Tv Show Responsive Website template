<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Vidstream - Movie & Tv Show Responsive Website template</title>

  <style>
    :root {
      --img: url("../flayer.jpeg");
    }

    html, body {
      height: 100%;
      margin: 0;
      background: #0b0f14;
      overflow: hidden;
      display: grid;
      place-items: center;
    }

    /* Fondo desenfocado (misma imagen) */
    body::before{
      content:"";
      position: fixed;
      inset: 0;
      background: var(--img) center/cover no-repeat;
      filter: blur(12px) brightness(0.8) saturate(1.15);
      transform: scale(1.06);
      z-index: -2;
      pointer-events: none;
    }

    /* Viñeta suave */
    body::after{
      content:"";
      position: fixed;
      inset: 0;
      z-index: -1;
      pointer-events: none;
      background: radial-gradient(900px 520px at 50% 45%,
        rgba(0,0,0,0) 35%,
        rgba(0,0,0,0.45) 100%
      );
    }

    /* Imagen centrada, sin recortar (contain). Las "franjas" ahora muestran el fondo desenfocado. */
    #mainImg{
      max-width: 100vw;
      max-height: 100vh;   /* <-- importante: 100vh para aprovechar toda la altura */
      width: auto;
      height: auto;
      object-fit: contain;  /* muestra la imagen completa */
      display: block;

      user-select: none;
      -webkit-user-drag: none;
      pointer-events: none;

      opacity: 0;
      transform: translateY(6px);
      transition: opacity .35s ease, transform .35s ease;
      z-index: 0;
    }

    #mainImg.loaded{
      opacity: 1;
      transform: none;
    }
  </style>
</head>

<body>
  <img id="mainImg" src="../flayer.jpeg" alt="Flyer cinematográfico">

  <script>
    const img = document.getElementById('mainImg');
    if (img.complete) img.classList.add('loaded');
    else img.addEventListener('load', () => img.classList.add('loaded'));
  </script>
</body>
</html>
